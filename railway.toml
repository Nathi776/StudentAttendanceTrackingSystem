[services.backend]
source = "backend"
buildCommand = "pip install -r requirements.txt"
startCommand = "gunicorn myserver.wsgi --bind 0.0.0.0:$PORT"

[services.frontend]
source = "frontend"
buildCommand = "npm install && npm run build"
startCommand = "npx serve -s build -l $PORT"